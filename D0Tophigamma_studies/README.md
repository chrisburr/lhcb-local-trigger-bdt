# Trigger rate studies for D0 -> phi gamma

## Running

To run for a single threshold:

```bash
./TriggerRateTest.sh $BDT_THRESHOLD
```

To run for a every threshold in a range (inclusive):
```bash
./TriggerRateTest.sh $START $STOP $STEP
```

